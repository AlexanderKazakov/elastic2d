reset
set term png

unset key
set ylabel "detector value"
set xlabel "time"
set grid


list = system('ls snapshots/1dseismo | grep bin')

do for [file in list] {
    set output sprintf('snapshots/1dseismo/%s.png', file)
    plot sprintf('snapshots/1dseismo/%s', file) binary record=(-1) using 0:1 with lines
}


